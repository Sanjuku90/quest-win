## Packages
framer-motion | Complex animations for roulette and page transitions
lucide-react | Icons for the UI (already in base but ensuring version)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Authentication is handled via Replit Auth (useAuth hook).
API endpoints are defined in shared/routes.ts.
The design should be "Professional fintech dashboard, gamified elements with gold/purple accents".
Mobile responsiveness is critical.
